#include <stdio.h>

int int main() {

int numRooms;
scanf("%d\n", &numRooms);

printf("There are %d rooms created.");

  return 0;
}


if (n != -1)
{
  room[i].north = rooms[n];
}
if (s != -1)
{
  room[i].south = rooms[s];
}
if (e != -1)
{
  room[i].east = rooms[e];
}
if (w != -1)
{
  room[i].west = rooms[w];
}
